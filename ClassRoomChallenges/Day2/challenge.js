/* BRONZE */

let myName = "Mello";
let name2 = "Delaney";


console.log(myName.length); //5
console.log(name2.length); //7

/* SILVER */

if (myName.length > name2.length) {
   
    console.log(myName.length, name2.length);
    console.log(`${myName} is shorter than ${name2}.`);
} else {
    console.log(`${myName} is shorter than ${name2}.`);
}

/* GOLD */
let numbers = myName.length - name2.length
    console.log(numbers)

    if (myName.length, name2.length) {
        console.log(`Delaney is longer than ${myName} by ${name2.length - myName.length} letters`)
    } else {
        console.log(`${myName} is longer than my friends by ${myName.length - name2.length}`)
    }

    //PLATINUM

    let myName = "Mello";
    let name2 = "Delaney";

